# Construction 프로젝트 코드 감사 보고서

- 감사일: 2026-08-14
- 대상: Vue/Vite 프런트엔드, 도면 분석·3D·산출·절단·재고 모듈, 선택형 Node API 서버
- 기준 브랜치: `main`
- 원칙: 근거가 없는 치수·높이·가격은 추정하지 않고, 검증을 통과하지 못한 결과는 발주용으로 내보내지 않습니다.

## 수정한 핵심 문제

### 도면 인식과 형상 연결

- `T.O.S.`, `T.O.F.`, `EL`, `FFL`, `GL`의 양수·음수 레벨 부호를 보존했습니다. 예를 들어 `+2,800mm - (-1,200mm)`를 `4,000mm` 높이로 계산합니다.
- 같은 숫자가 한 문장에 반복되어도 각 숫자의 실제 위치를 기준으로 단위와 역할을 연결합니다. `폭 300cm, 높이 300mm`를 모두 `cm`로 오인하던 문제를 막았습니다.
- PDF.js의 실제 `OPS` 상수와 typed array를 사용해 `constructPath` 벡터 선분을 읽도록 수정했습니다.
- 한 구역의 개구부를 모든 벽에 복제하지 않고 명시된 벽 번호·축에만 연결합니다. 대상이 모호하면 자동 차감하지 않고 검토 항목으로 남깁니다.
- 혼합 PDF의 도면·공사비·분류 미정 페이지를 파일 전체가 아닌 페이지 단위로 분리해, 도면 치수가 비용으로 빠지거나 비용 숫자가 geometry에 들어가지 않게 했습니다.
- 벽 번호가 명시된 높이는 해당 벽에만 연결하고, 여러 레벨 쌍·다른 구역 개구부·반복 offset처럼 1:1 근거가 아닌 값은 자동 확정하지 않습니다.
- 벽 밖으로 나간 문·창호는 벽과 겹치는 면적만 차감하고, 경계 이탈 사실을 검토 상태로 표시합니다.
- OCR 재실행 조건을 바로잡아 높이 숫자가 없을 때만 분할 OCR을 보강하고, 이미 `H=3000`처럼 충분한 결과가 있으면 불필요한 반복을 피합니다.

### 자재 산출, 절단 최적화, 재고

- 순 벽체 면적을 자기 자신과 비교하던 일관성 검사를 실제 벽 geometry와 다시 계산한 값의 비교로 교체했습니다.
- 사용자 승인 높이가 원본과 같아도 불일치로 표시되던 오류와, 기존 현장 자투리 전체를 새 폐기물로 중복 계산하던 오류를 수정했습니다.
- 복수 자재를 목적별 3개 시나리오로 통합해 한 자재만 포함된 계획이 `calculated`로 통과하지 못하게 했습니다.
- 부재 `quantity`를 실제 배치 수에 반영하고, 부재·보유 재고·예약 수량은 1 이상의 정수 또는 0 이상의 정수만 허용합니다.
- 판재의 길이와 폭을 모두 줄이면 직교 절단 2회로 계산합니다.
- 판재·프로파일 모두 절단폭을 포함한 exact-fit 경계를 적용하고, 원자재 가장자리 잔량이 톱날 폭보다 작으면 배치 불가로 처리합니다.
- 음수 가격·비용·절단폭과 소수 최소 주문 수량 등 잘못된 카탈로그 값은 발주 가능 상태로 통과하지 못합니다.
- 부재 형태와 카탈로그 형태가 다르거나, 길이별 견적 없이 서로 다른 원자재 길이를 비용 비교하면 확인 필요로 차단합니다.
- 승인한 보유 자재 계획을 취소할 때 예약 수량을 원자적으로 되돌리는 동작과 화면 버튼을 추가했습니다.
- 복수 자재 발주 CSV는 실제 선택된 원자재 규격별 수량·MOQ·비용을 따로 계산하고 마지막에 전체 합계를 표시합니다.
- 기존 현장 자투리의 입력 원본과 계산 중 소비 상태를 분리해, 같은 입력으로 다시 계산해도 현장 자투리 사용·신규 발주 수량이 바뀌지 않게 했습니다. 카탈로그 ID가 같아도 재질·두께가 달라진 자투리는 자동 사용하지 않습니다.
- 승인된 보유 재고 계획은 신규 발주서뿐 아니라 실제 보유 자재의 절단 순서·잔량을 담은 전용 CSV로 출력합니다. 이 상태에서는 재고를 반영하지 않은 원자재 배치도·자투리 목록 인쇄를 현장 실행용으로 잠급니다.

### 화면 흐름, 저장, 접근성, 성능

- 빈 프로젝트에서 3D/최적화 화면으로 이동해 본문이 사라지던 상태 전이를 막고, 초기화·마지막 파일 삭제 후 업로드 화면으로 복귀시켰습니다.
- 업로드와 재분석에 공통 busy guard와 `try/finally`를 적용하고, 분석 중 삭제·초기화·중복 분석을 차단했습니다.
- 도면·치수·자재 설정이 바뀌면 예전 최적화와 재고 계획을 무효화합니다. 승인 예약은 자동 해제하지 않고, 명시적인 `예약 해제·계획 취소` 전에는 관련 입력 변경을 막아 감사 기록을 보존합니다.
- “검토 완료”와 “부분 3D 미리보기”를 분리했습니다. 검토가 남은 부분 모델은 자재 계산·발주 출력으로 진행할 수 없습니다.
- 최적화 결과뿐 아니라 도면→벽체→3D→산출의 전체 일관성 검증과 발주 가능 판정을 모두 통과한 경우에만 발주 CSV와 인쇄/PDF 기능을 엽니다.
- 최적화·재고 계획에는 입력 지문을 저장합니다. 도면, 자재 기준, 기존 자투리, 재고 요구사항이 달라지면 이전 계산·선택안·발주 출력은 즉시 잠기며 다시 계산해야 합니다.
- 자재 카탈로그, 선택 시나리오, 재고 요구사항, 현장 자투리 등 새로고침 시 유실되던 주요 변경을 즉시 저장합니다.
- 키보드 초점, `aria-current`/`aria-pressed`/`aria-expanded`, 진행률 의미, 클릭 표의 실제 버튼, 모션 감소 설정을 보강했습니다.
- PDF/OCR과 Three.js 뷰어를 필요할 때 지연 로딩해 초기 메인 JS를 약 1.4MB에서 약 356kB로 줄였습니다.

### 비용·출력·API 보안

- 월별 여러 금액이 있는 비용표는 각 행의 마지막 합계를 더하도록 수정하고, 업체명·전화·팩스·이메일·담당자·주소·계좌·등록번호와 비용 페이지 이미지를 로컬 저장본에서도 비식별화합니다.
- CSV 수식 주입과 인쇄 팝업의 HTML 삽입을 이스케이프합니다.
- API 서버는 기본 `127.0.0.1` 바인딩, Host allowlist, 고정 URL 기준 경로 파싱, 잘못된 Host의 400 처리, 최상위 예외 방어를 적용했습니다.
- IPv6 loopback(`::1`)도 Host allowlist에서 안전하게 처리합니다.
- 원천 API에 redirect 차단·전체 8초 deadline·응답 크기 상한을 두고, 성공 응답 캐시·동일 요청 병합·IP 요청 제한과 신뢰 프록시 체인 검증을 추가했습니다.
- 제공기관 URL은 공식 HTTPS 호스트만 허용하고, 클라이언트 링크는 `http(s)`만 통과시킵니다.
- 지방재정 오류 응답을 실제 계약으로 표시하지 않으며, 계약·국회·DART 응답은 화면에 필요한 공개 필드만 전달합니다.
- DART·국회 API 기능 플래그를 예제 환경에서 기본 비활성화하고, `nanoid` 취약 버전을 안전한 버전으로 갱신했습니다.
- 울산 이차전지 공고와 경기 에너지 사업의 상세 출처·게시일을 원문 기준으로 교정했습니다.

## 추가한 회귀검사

- `scripts/verify-domain-regressions.ts`: 부호 있는 레벨, 반복 숫자·혼합 단위, 벽·축·개구부 연결, 혼합 PDF, 경계 차감, PDF 벡터, 비용 합계·저장 비식별화
- `scripts/verify-regressions.ts`: OCR 정책, 복수 자재·수량, 2D/절단폭 경계, 카탈로그 형태·가격 경계, 자투리·순면적 일관성, 예약 해제, 실제 발주 규격, CSV 수식 주입
- `scripts/verify-server-boundaries.mjs`: 서버 바인딩·Host, 공식 URL, 제공기관 오류, 필드 최소화, 안전한 링크, 캐시·동시 요청 병합, 직접/신뢰 프록시 요청 제한

전체 확인 명령은 다음과 같습니다.

```sh
npm run check
npm audit --omit=dev --audit-level=moderate
git diff --check
```

## 남은 제한사항

- DWG, DXF, IFC 파서는 아직 연결되지 않았으며 PDF/JPG/PNG만 자동 분석합니다.
- OCR 결과는 원본 해상도·폰트·손글씨와 브라우저 환경에 영향을 받으므로 원본 도면과 현장 실측 확인이 필요합니다.
- 3D 지연 청크는 약 586kB로 Vite 권장 크기 경고가 남지만 초기 화면에는 포함되지 않습니다.
- 프로젝트 도면과 미리보기는 브라우저 `localStorage` 용량과 동기 저장 특성의 영향을 받습니다. 대형 프로젝트는 IndexedDB/Blob 분리가 후속 과제입니다.
- 구조용 프로파일 부재를 도면에서 자동 생성하는 파서는 아직 연결되지 않아 화면에서 프로파일 자동 발주를 만들지 않습니다.
- 서로 다른 원자재 길이는 길이별 단가 구조가 없으므로 총비용 최소안으로 확정하지 않고, 한 규격 선택 또는 실제 견적 확인을 요구합니다.
- 실제 API 키가 없어 제공기관 운영 스키마까지의 end-to-end 검사는 수행하지 못했습니다.
- 서버 캐시와 요청 제한은 단일 프로세스 메모리 기반입니다. 다중 인스턴스 운영에서는 공유 캐시/API Gateway가 필요합니다.
- 해남 데이터센터 항목의 `40MW` 수치는 현재 연결된 전남 보도자료에서 직접 확인되지 않아 별도 필드 출처가 필요합니다.
- 이 결과는 자재 산출용 보조 자료이며 구조검토, 설계 승인, 제조사 시방, 현장 실측을 대신하지 않습니다.
